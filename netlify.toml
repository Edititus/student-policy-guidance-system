
[build]
  command = "npm run build --workspace=apps/frontend"
  publish = "apps/frontend/dist"

# Redirect API calls to your Render backend (replace with your actual backend URL after deployment)
[[redirects]]
  from = "/api/*"
  to = "https://backend-3esp.onrender.com/api/:splat"
  status = 200
